<Window x:Class="MinimarketApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MinimarketApp"
        xmlns:view="clr-namespace:MinimarketApp.View"
        xmlns:userControls="clr-namespace:MinimarketApp.View.UserControls" d:DataContext="{d:DesignInstance Type=userControls:TextBox}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <userControls:MenuBar Grid.Row="0"/>

        <view:Login Grid.Row="1" Width="200" Height="220"/>
    </Grid>
</Window>
